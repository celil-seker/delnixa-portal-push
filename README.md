# Delnixa Portal

## Kurulum

git clone https://github.com/celil-seker/delnixa-portal-push.git
cd delnixa-portal-push
./setup.sh
docker compose up -d --build

Tarayıcıdan https://localhost adresine git (self-signed sertifika uyarısını kabul et).
